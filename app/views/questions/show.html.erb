<h1>Question of test: <%= @test.title %></h1>
<p><%= @question.body %></p>

<%= link_to "Destroy", question_path(@question), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"} %>