<h1>Question of test: <%= @question.test.title %></h1>
<p><%= @question.body %></p>

<ul>
  <li><%= link_to "Destroy", question_path(@question), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"} %></li>
  <li><%= link_to "Edit", edit_question_path(@question) %></li>
  <li><%= link_to "Show proper test", test_path(@question.test)%></li>
</ul>
